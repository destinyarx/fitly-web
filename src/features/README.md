# Features

Create one directory per business capability. A feature owns its components, hooks, services, schemas, types, query keys, constants, stores, and public `index.ts`.

Other features must import through that public entry point. Do not create empty feature folders before implementation starts.
